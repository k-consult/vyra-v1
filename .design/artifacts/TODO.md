# Artifacts TODO

- [ ] Document the `Task → Control → Requirement → Regulation` traceability flow (shown in `architecture-v2.html`'s Layer 2 · Traceability) in the canonical docs: add it as a third traversal pattern in `vyra-graph-spine.md` Part IV (alongside the existing forward/reverse patterns), and note it against the L3 Planning row in `vyra-tracker.md`.
