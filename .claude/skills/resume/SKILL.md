Read .design/roadmap.md and identify the next unstarted phase. Implement it end-to-end across schema, ingest, API, and UI. Before reporting completion, you MUST: (1) run typecheck and fix all errors, (2) query the live Neo4j graph via cypher-shell to confirm nodes/relationships were created correctly, (3) hit each new API endpoint and confirm the response is unwrapped and well-formed (no [object Object]), and (4) verify the data renders in the actual UI page. List each verification with its evidence, then commit with a descriptive message. Do not declare success on any layer you have not independently proven against the running system.