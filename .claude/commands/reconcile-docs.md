Spawn three parallel subagents to reconcile my documentation with reality. Agent 1: audit every doc in .design/ against the current code and recent git commits, listing each factual claim that has drifted (especially any UI copy telling users to 'run' something that is actually idempotent/no-op). Agent 2: map every graph entity and API endpoint in the codebase to the doc that describes it, flagging undocumented and orphaned items. Agent 3: propose a consolidation into 3 canonical docs. Merge their findings into a single prioritized reconciliation report with exact file/line references, and stage the consolidation edits for my review without committing.
