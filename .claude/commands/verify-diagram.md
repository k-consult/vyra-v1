Build a verification harness for my HTML architecture diagram using Playwright. Write assertions that load the page headless and confirm: the specified color palette values are actually applied to the right elements, the custom font is loaded (not falling back to a serif), CSS variables are declared inside valid rule blocks, and the layout/loop structure matches my spec. Then implement my requested design changes and run the harness in a loop, fixing failures until every assertion passes. Capture a before/after screenshot and report the passing checks.

$ARGUMENTS
